{
  "name": "my-webpack-wasm-package",
  "version": "0.0.1",
  "description": "wasm",
  "scripts": {
    "build-linux": "export NODE_OPTIONS=--openssl-legacy-provider && rimraf dist && webpack",
    "start-linux": "export NODE_OPTIONS=--openssl-legacy-provider && rimraf dist && webpack-dev-server --open",
    "build-windows": "set NODE_OPTIONS=--openssl-legacy-provider && rimraf dist && webpack",
    "start-windows": "set NODE_OPTIONS=--openssl-legacy-provider && rimraf dist && webpack-dev-server --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "your@<account>",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.21.0",
    "@babel/plugin-syntax-dynamic-import": "^7.8.3",
    "@babel/preset-env": "^7.20.2",
    "@wasm-tool/wasm-pack-plugin": "^1.6.0",
    "babel-loader": "^9.1.2",
    "copy-webpack-plugin": "^11.0.0",
    "rimraf": "^4.3.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.11.1"
  }
}
